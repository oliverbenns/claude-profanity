name: Publish Skill

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Publish skill
        run: gh skill publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
